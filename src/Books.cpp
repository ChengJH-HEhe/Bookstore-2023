#include "Books.h"

namespace Books_system {
  
void read(std::istringstream &stream, char c1) {
  string s[5] = {"@", "@", "@", "@", "@"};
  int sz = 0;
  while (stream >> s[sz++]);

}
void Init();
} // namespace Books_system